// models/coach.js

const mongoose = require('mongoose');

const coachSchema = mongoose.Schema({
    // Définir la structure des données pour un coach
});

module.exports = mongoose.model('Coach', coachSchema);
